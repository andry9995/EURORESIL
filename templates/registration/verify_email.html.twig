{% extends 'base_public.html.twig' %}
{% block title %}Verifiez votre email — EURORESIL{% endblock %}
{% block body %}
    <div style="min-height:100vh;display:flex;align-items:center;justify-content:center;padding:20px">
        <div class="auth-card" style="width:100%;max-width:420px">
            <h3>Verifiez votre email</h3>
            <p class="desc">Nous avons envoyé un code de vérification à 6 chiffres à votre adresse e-mail.</p>
            {% for type, messages in app.flashes %}
                {% for msg in messages %}
                    <div class="flash flash-{{ type }}">
                        {{ msg }}
                    </div>
                {% endfor %}
            {% endfor %}
            <form method="post" id="codeForm">
                <input type="hidden" name="_csrf_token" value="{{ csrf_token('verify_email') }}">
                <input type="hidden" name="code" id="codeHidden">
                <div class="code-row">
                    <input class="code-input" maxlength="1" inputmode="numeric">
                    <input class="code-input" maxlength="1" inputmode="numeric">
                    <input class="code-input" maxlength="1" inputmode="numeric">
                    <input class="code-input" maxlength="1" inputmode="numeric">
                    <input class="code-input" maxlength="1" inputmode="numeric">
                    <input class="code-input" maxlength="1" inputmode="numeric">
                </div>
                <button class="btn btn-primary btn-block" style="margin-top:18px" type="submit">Valider</button>
            </form>
            <p class="code-note">
                Vous n'avez pas reçu le code ? <a class="auth-link" href="{{ path('app_resend_verification_email') }}">Cliquez
                    ici</a> pour le renvoyer. </p>
        </div>
    </div>

    <script>
        const inputs = document.querySelectorAll('.code-input');

        inputs.forEach((el, i, arr) => {
            el.addEventListener('input', () => {
                if (el.value && arr[i + 1]) arr[i + 1].focus();
            });

            el.addEventListener('keydown', (e) => {
                if (e.key === 'Backspace' && !el.value && arr[i - 1]) {
                    arr[i - 1].focus();
                }
            });

            el.addEventListener('paste', (e) => {
                e.preventDefault();

                const pasteData = (e.clipboardData || window.clipboardData).getData('text').replace(/\D/g, '');

                inputs.forEach((input, index) => {
                    if (pasteData[index]) {
                        input.value = pasteData[index];
                    }
                });

                const focusIndex = Math.min(pasteData.length, inputs.length - 1);
                inputs[focusIndex].focus();
            });
        });

        document.getElementById('codeForm').addEventListener('submit', function () {
            document.getElementById('codeHidden').value = [...inputs].map(e => e.value).join('');
        });
    </script>
{% endblock %}